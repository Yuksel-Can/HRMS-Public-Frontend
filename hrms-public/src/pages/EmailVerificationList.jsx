import React from 'react'
import { useState,useEffect } from 'react';
import EmailVerificationService from "../services/emailVerificationService";

export default function emailVerificationList() {
    /*
    const [emailVerifications, setEmailVerifications] = useState([])

    useEffect(()=>{
        let emailVerificationService = new EmailVerificationService();
        emailVerificationService.getAllEmailVerification.then(result=> setEmailVerifications(result.data.data))
    })
*/
    return (
        <div>
            
        </div>
    )
}
